---
description: Change existing content on the landing page
argument-hint: what you want to change
---

You are editing the Moya Rituals landing page. Make the following change:

**$ARGUMENTS**

Follow these steps:
1. Identify which file needs editing. For any text or copy change, start with `lib/constants.ts` — it exports nearly all brand content.
2. Read the file before editing.
3. Make ONLY this change — no cleanup, no extra edits.
4. Confirm: what file was changed, what the old value was, and what the new value is.

Refer to the routing table in `.claude/agents/landing-page-editor.md` if you're unsure which file to target.
